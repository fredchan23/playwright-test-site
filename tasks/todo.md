# Active Tasks

No active tasks. Three plans are queued for follow-up sessions (listed in priority order):

---

## Ready

- **Important fixes** (`plan-important-fixes.md`) — 2 tasks: refactor `handleDrop` fake-event cast; add `file_data` size cap in edge function. ~1 hour.
- **Suggestion fixes** (`plan-suggestions.md`) — 7 items in 3 clusters: code quality (test sleep, forEach→find, shimmer style); edge function config (GCP env vars, SA key guard); CORS + test env. ~1.5 hours.

## On Hold

- **Mobile E2E coverage** (`plan.md`) — 3 tasks, 6 new tests for mobile drawer, tabs, icon-only buttons. Awaiting go-ahead.
